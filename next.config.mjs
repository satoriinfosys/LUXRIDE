/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fastly.picsum.photos', 'localhost', 'parash-limo-backend.onrender.com'], // Add the required domain here
    },
};

export default nextConfig;
